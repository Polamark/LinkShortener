# 🔗 PHP Link Shortener

A lightweight URL shortening service built with PHP.

---

## ▶️ To Run:

1. Install dependencies:
   ```bash
   npm install

2. Ensure you have a service capable of running PHP code, such as:

    * PHP’s built-in development server
    * Apache or Nginx with PHP enabled
    * A local development environment (e.g. XAMPP, MAMP)

3. Start your PHP server and access the project through your browser.
4. Enjoy!

## 🎨 Built With Tailwind CSS

This project uses Tailwind CSS for styling.  
To continue building and watching for changes, run:

```bash
npx @tailwindcss/cli -i ./src/main.css -o ./src/output.css --watch
